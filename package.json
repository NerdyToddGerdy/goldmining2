{
  "name": "gold-prospecting",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "package:itch": "npm run build && rm -f itch.zip && cd dist && zip -r ../itch.zip ."
  },
  "dependencies": {
    "pixi.js": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "~5.9.3",
    "vite": "^6.4.3",
    "vitest": "^4.1.11"
  }
}
